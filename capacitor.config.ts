import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.github.espositoalessandro.lootrack",
  appName: "Lootrack",
  webDir: "dist/lootrack/browser",
};

export default config;
